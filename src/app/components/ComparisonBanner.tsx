/** Legacy React homepage version switcher — disabled; static site is canonical. */
interface ComparisonBannerProps {
  currentVersion?: string;
}

export function ComparisonBanner(_props: ComparisonBannerProps) {
  return null;
}
